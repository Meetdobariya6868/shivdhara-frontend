import React from 'react';
import { Login } from '@/pages/Login';
// Import other pages as you create them

export const AppRoutes: React.FC = () => {
  // For now, just return Login page
  // Later you can add react-router-dom here
  return <Login />;
};